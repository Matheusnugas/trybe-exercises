const upperCaseStr = (str) => str.toUpperCase();
const firstCharacter = (str) => str.charAt(0);
const concatenate = (str1, str2) => str1.concat(str2);

module.exports = {  upperCaseStr, firstCharacter, concatenate };